# Copyright (c) 2025-2026 Mark Buckwell, Zensical and contributors
# SPDX-License-Identifier: MIT

# File to list prerequisites for the project. This file is used by the CI/CD pipeline to install dependencies before building the documentation.
# >=0.0.53 confirmed byte-for-byte identical PDF, and identical search.json,
# against 0.0.52 for this project's own content - the only differences are
# the generator meta tag and rehashed asset filenames from a rebuilt
# bundle/CSS. >=0.0.52 itself was confirmed the same way against 0.0.51 -
# the only differences there were a bundled Font Awesome/Lucide icon-set
# bump (redrawn SVG artwork, new asset hashes) and an additive "lang" field
# in search.json's config.
zensical>=0.0.53
# A floor here, same reasoning as zensical and weasyprint below - CI pins it
# exactly instead (see docs.yml), since prodockit is what renders this
# project's own PDF and generates its index (issue #149). Kept at the same
# version as the exact pin, like the other two - `prodockit pins --check`
# treats a floor behind the exact pin as drift, not merely a wider
# allowance. >=0.13.0 would be the true functional minimum (`prodockit
# sync-repo`, which replaced sync_repo_icon.py); >=0.15.2 added the
# Mermaid/TeX checks' font-ligature handling this project discovered (issue
# #128); >=0.17.2 added `prodockit pins` itself (see
# .github/workflows/docs.yml's own pin lines, and CONTRIBUTING.md), which
# manages the exact zensical/weasyprint/prodockit pins below and in CI
# together, rather than by hand across several files.
#
# 0.20.1 is different from those: it is a genuine functional minimum for
# *this* template rather than a floor tracking the exact pin. The pipelines
# here pin PANDOC_VERSION 3.10.1, and on that pandoc every prodockit before
# 0.20.1 loses the `<pre>` around each fenced code block - the block then
# reflows as justified prose in the PDF while the build reports success
# (prodockit-extensions#207). A project generated from this template
# inherits the pandoc pin, so it inherits that floor with it.
prodockit>=0.20.1
mkdocstrings-python
pandoc
toml
types-toml
# A floor here, same reasoning as zensical above - CI pins it exactly
# instead (see docs.yml), since layout/pagination is the one thing this
# project's own published PDF needs to hold still between builds.
weasyprint>=69.0
beautifulsoup4
emoji
