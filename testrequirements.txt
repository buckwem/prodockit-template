# Copyright (c) 2025-2026 Mark Buckwell, Zensical and contributors
# SPDX-License-Identifier: MIT

# File to list the test suite's own prerequisites (see test/), kept separate
# from requirements.txt so someone building their own report from this
# template never needs to install them - only contributors/CI running
# `python test/run_tests.py` do. Install alongside requirements.txt, since
# the tests also rely on beautifulsoup4 (already declared there):
#   pip install -r requirements.txt -r testrequirements.txt

# Brings pytest and pymupdf, plus prodockit.testing - whose Mermaid/TeX
# checks test/test_pdf_rendering.py now uses instead of its own copy
# (issue #128).
prodockit[testing]>=0.48.0
